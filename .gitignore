# Рабочие черновики генерации кода
scripts/ollama/

# Рабочий список задач: содержит внутренние пути контура и суммы реальных смет
TODO.md

# Сметы и любые данные заказчиков — в репозиторий не попадают никогда
*.xlsx
*.sobx
# Исключение: синтетический донор для тестов генератора. Собран контуром «Сметчик»
# из выдуманных позиций, паспорт архива обезличен (SysID/имя ПК/учётка — заглушки).
# Проверен независимо перед публикацией: маркеров реального объекта нет, MAC-адресов
# нет (все 12-значные hex — хвосты GUID), путей Windows и ФИО нет.
!tests/fixtures/synthetic-donor.sobx
*.arp
*.arps
sample_data/
# Отчёты сравнения: содержат шифры, наименования и суммы из смет
smeta-compare*.txt
*-compare-report.txt

__pycache__/
*.py[cod]
.pytest_cache/
build/
dist/
*.spec.bak
.venv/
.idea/
.vscode/
