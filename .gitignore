# Рабочие черновики генерации кода
scripts/ollama/

# Рабочий список задач: содержит внутренние пути контура и суммы реальных смет
TODO.md

# Сметы и любые данные заказчиков — в репозиторий не попадают никогда
*.xlsx
*.sobx
# Донор для тестов генератора НЕ публикуется: в справочнике наименований B_NNAME
# синтетического файла осталось 135 позиций с закупочными ценами реальной сметы
# («… Цена=23 988») — они наследуются вместе со справочником, даже когда позиции
# и паспорт архива обезличены. Проверять .sobx нужно и на цены в наименованиях,
# а не только на имена объекта, MAC-адреса и ФИО.
*.arp
*.arps
sample_data/
# Отчёты сравнения: содержат шифры, наименования и суммы из смет
smeta-compare*.txt
*-compare-report.txt

__pycache__/
*.py[cod]
.pytest_cache/
build/
dist/
*.spec.bak
.venv/
.idea/
.vscode/
